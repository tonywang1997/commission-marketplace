if (<%= j @sort == "none" %>) {
  $('.btn.btn-direction').addClass('disabled');
} else {
  $('.btn.btn-direction').removeClass('disabled');
}

<% @images.each_with_index do |img, idx| %>
  $('#img-<%= img[:id] %>').attr('data-order', '<%= j idx %>');
<% end %>

window.orderImages();